const reactClass = require('./reactClass');
const vueTemplate = require('./vueTemplate');
const reactReduc = require('./reactReduc');

module.exports = 
{
  reactClass: reactClass,
  vueTemplate: vueTemplate,
  '基于react的redux物料模版v1.0': reactReduc,
}
